#pragma once

#include <sstream>
#include <string>
#include <string_view>
#include <cstring>

namespace std {

    using String = string;
    
    using StringView = string_view;

    using StringStream = stringstream;

}